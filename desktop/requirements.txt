# DinGelSchwinG Agent Console v3.0 – Abhängigkeiten
# Pflicht (GUI):
customtkinter>=5.2.0

# Optional – Live-Status über WebSocket (/ws/status):
websocket-client>=1.6.0

# Optional – lokales Embedded-Modell (GGUF, z.B. Qwen2.5-0.5B-Instruct):
# Installation:  pip install llama-cpp-python
# Modell laden:  python tools/download_model.py

# Optional – wenn das Backend auf Flask + flask-socketio läuft (Server-Seite):
# flask, flask-socketio, flask-cors

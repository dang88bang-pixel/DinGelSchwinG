// lib/features/agent/chat_message.dart
// Chat-Nachrichtenmodell (Dünner Wrapper um AgentMessage des Services).
import '../../core/agent/agent_service.dart';

typedef ChatMessage = AgentMessage;

typedef ChatSender = Sender;

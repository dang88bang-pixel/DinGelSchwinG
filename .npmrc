# onnxruntime-node lädt beim Installieren native Binaries von externen CDNs.
# Die App nutzt nur onnxruntime-web (WASM, Browser/WebView) – daher werden
# Install-Skripte von Abhängigkeiten übersprungen (npm ci bleibt reproduzierbar).
ignore-scripts=true

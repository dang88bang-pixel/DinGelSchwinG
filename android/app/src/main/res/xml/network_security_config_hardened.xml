<?xml version="1.0" encoding="utf-8"?>
<!--
  Gehärtete Variante: http NUR für Loopback/Emulator und Namenszonen.
  Aktivieren mit android:networkSecurityConfig="@xml/network_security_config_hardened"
  im <application>-Tag. Voraussetzung: der Mobile-Server läuft hinter TLS
  (siehe deploy/ + server.crt im Repository) oder ist über .local/.lan erreichbar.
  Werks-IPs wie 192.168.x.x müssen dann als <domain>-Eintrag ergänzt werden.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">*.local</domain>
        <domain includeSubdomains="true">*.lan</domain>
        <domain includeSubdomains="true">*.home.arpa</domain>
    </domain-config>
</network-security-config>

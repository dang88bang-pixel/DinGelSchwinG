<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme only; the actual look is driven by Jetpack Compose. -->
    <style name="Theme.GenesisOrchestrator" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
